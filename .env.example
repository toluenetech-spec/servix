# SERVIX frontend — environment variables (all optional)

# Live API base URL. When unset, the site uses bundled demo data and the
# auth pages show honest pre-launch messaging. When set, catalogue data
# comes from the API (with demo fallback) and authentication is real.
# VITE_API_URL=http://localhost:8080
